import jax.numpy as jnp
import numpy as np

# Get all handcrafted graphs -> Zero pad to make them 10 nodes -> Save in the same format as inference graphs
# Delauney graphs with 5,7,9 nodes and zero pad to be 10 ->  Save in the same format as inference graphs
# Final: 4 separate folders


def zero_pad_graphs(stored_graphs: jnp.ndarray) -> jnp.ndarray:
    pass
